apiVersion: v1
kind: Service
metadata:
  name: { { include "office-llm-harness.fullname" . } }
  labels: { { - include "office-llm-harness.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "office-llm-harness.selectorLabels" . | nindent 4 } }
