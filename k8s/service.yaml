apiVersion: v1
kind: Service
metadata:
  name: office-llm-harness
  labels:
    app: office-llm-harness
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: office-llm-harness
